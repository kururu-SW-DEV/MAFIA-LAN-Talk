# v1.27 — 전체 빌드 총점검 + 이상 문자 정리 (2026-09-18)

## 작업 (유저 요청 "지금까지 빌드 검토하고 버그 수정")

전 모듈 상태 총점검 + v1.00~v1.26 누적 코드의 잔여 결함 정리.

## 점검 결과

| 항목 | 결과 |
|---|---|
| 컴파일(py_compile) 전 모듈 | PASS (config/core/ai/net/chat_renderer/dialogs/app/ui) |
| 핵심 심볼 존재 | _sync_ai_alive 3지점 / _kick_silent_room / _poll_defense_ui_queue / _start_defense_votes / 폴백 타이머 취소 — 전부 OK |
| 소스 이상 문자(한자 삽입) | **1건 발견·수정** — mafia_ui.py 692행 `나同年 사회자` → `내가(나) 사회자` (v1.12 패치 시 유입) |
| core enter_night(페이즈 상태머신) | DAY/VOTE 진입 허용, 밤 진입 후 액션 초기화 정상 |
| 배포 | 재빌드 30.05MB → Desktop 복사 + 스모크 PASS |
