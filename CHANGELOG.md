# 변경 이력

버전별 주요 변경 사항을 간단히 정리합니다.

## v6.49 (2026-09-18)
- **[버그] 일반(비-마피아) 1:1·그룹 채팅 전송이 아예 안 되던 문제 수정**: 마피아
  게임방 통합 패치 과정에서 `_send()`의 일반 채팅 분기가 입력창 내용을 변수에
  담기 전에 먼저 지워버리는 실수가 들어가, 메시지를 보내려 하면 매번
  `UnboundLocalError`로 조용히 실패하고 있었다(콘솔 없는 창모드라 사용자에게는
  그냥 "전송이 안 되는" 것처럼만 보였다). 마피아 게임 투표 흐름을 회귀
  테스트하다가 발견해 즉시 수정 — 이제 1:1·그룹 텍스트/파일 전송 모두 정상
  동작한다.

## v6.48 (2026-09-16)
- **채팅 말풍선을 전부 좌측 정렬로 통일**: 내 메시지(파란색)·상대 메시지(회색)
  모두 왼쪽에서 시작하도록 바꾸고 색으로만 구분한다(텍스트·사진·파일카드·
  스티커·인용 답장·시간 표시 전부 동일하게 적용).
- **[버그] 상대 이름이 오프라인이 되거나 앱을 재시작하면 IP로 되돌아가던
  문제 수정**: 상대의 실제 표시 이름을 한 번 확인하면 계속 기억해서(설정에
  영속 저장), 다시 오프라인이 되거나 앱을 재시작해도 이름이 유지된다.
  프로필 색상이 이름 문자열을 기준으로 정해지는데 이름이 IP로 바뀔 때마다
  같이 바뀌어 "색이 랜덤하게 바뀐다"고 보이던 것도 같은 원인이라 함께 해결됨.
- **사이드바 접기/펼치기 애니메이션 개선**: 애니메이션 중 대화 목록(피어마다
  여러 개의 실제 위젯으로 구성)을 통째로 움직이던 방식 대신, 자식 위젯이
  없는 가벼운 배경색 패널만 슬라이드시키도록 바꿔 훨씬 매끄러워졌다. 또한
  애니메이션 시작·종료 시점에 채팅 영역 크기 변경 감지가 중복으로 걸려
  채팅을 두 번 다시 그리면서 스크롤 버튼 같은 위젯이 깜빡이던 것도 수정.
- 좌측 사이드바 헤더의 중복 접기 버튼(◀)을 제거(분할선 버튼·`Ctrl+B`·`F4`·
  분할선 더블클릭으로 여전히 접고 펼 수 있음).
- **[버그] 방화벽 자동 등록이 항상 실패하던 문제 수정**: 이름에 공백이 있는
  ("LAN Talk") 방화벽 규칙을 관리자 권한 상승 후 등록할 때, 인자를 단순
  공백으로만 이어붙여서 Windows가 "LAN"과 "Talk"를 서로 다른 인자로 잘못
  쪼개 매번 조용히 실패하고 있었다. 올바르게 따옴표를 씌우도록 수정.
- **[버그] 방화벽 자동 등록 시 화면이 잠깐 멈췄다가 문구가 갑자기 나타나는
  문제 수정**: 관리자 권한 승인 대기를 메인 화면 스레드에서 그대로 기다리고
  있어서 그 몇 초 동안 화면이 멈춘 것처럼 보였다. 백그라운드로 옮겨 화면이
  계속 정상적으로 그려지도록 수정.
- **기본 파일 전송 최대 크기를 20MB → 1024MB(1GB)로 상향**: 양쪽 다 이 값
  이상이어야 그 크기 파일을 주고받을 수 있는데, 기본값이 낮으면 대용량 전송이
  늘 막혀서 매번 양쪽 설정을 먼저 맞춰야 했다(설정에서 1MB까지 낮추는 것은
  여전히 가능).
- **[버그] 200MB급 대용량 파일이 상대방에게 전혀 전달되지 않던 문제 수정**: 받는
  쪽의 [파일전송 다운로드 최대크기] 설정보다 큰 파일을 보내면, 예전엔 받는 쪽이
  조용히 무시만 해서 보내는 쪽은 "전송 중"으로 계속 진행되다 뒤늦게 실패로
  끝났다. 이제 받는 쪽이 거절 사유(최대 크기)를 명시적으로 돌려주고, 보내는
  쪽은 그 즉시 전송을 멈추고 "상대방의 파일 수신 최대 크기 설정(XXXMB)보다
  커서 거절되었습니다"라고 구체적으로 안내한다.
- **[버그] 포트 번호를 기본값(50707)이 아닌 값으로 바꿔 쓸 때 파일 이어받기가
  깨지던 문제 수정**: 위 기능을 만들다가 발견 — 받는 쪽의 이어받기 응답
  패킷에 자기 포트 정보가 빠져 있어, 발신자가 그 응답을 못 찾고 매번 처음부터
  다시 보내고 있었다.
- **[버그] 받은 파일을 다운로드 폴더에서 직접 지우면 채팅창에 "전송 중…"이
  계속 표시되던 문제 수정**: 실제로는 이미 정상 수신된 파일인데도 파일 존재
  여부만으로 판단해 영원히 "전송 중"으로 보이던 것을, 수신 완료 여부를 별도로
  기록해 "파일 없음(삭제되었거나 이동됨)"으로 정확히 표시하도록 고쳤다.
- **방화벽 예외 자동 등록**: 처음 실행할 때 "방화벽 예외를 자동으로
  등록할까요?"라고 물어보고, 동의하면 관리자 권한 승인을 거쳐 `netsh`로 UDP
  포트 인바운드 규칙을 직접 등록한다(기존엔 안내 문구만 있었음).
- 대화방을 오래 켜놓고 이미지를 많이 주고받아도 메모리가 무한정 늘지 않도록
  캔버스 이미지 참조에 상한을 뒀다.

## v6.47 (2026-09-16)
- 팝업 알림 조건에서 작업표시줄이 계속 점멸하도록 개선(타이머 재점멸)
- 알림 클릭 시 앱이 항상 맨 앞으로 오고, 해당 대화방으로 바로 이동하도록 수정
- 액션 센터 토스트 클릭이 OS 레벨에서 불안정하게 씹히는 문제를 우회하기 위해
  인앱 알림 팝업(슬라이드 등장 애니메이션, 매끄러운 원형 아바타) 도입 — 트레이
  풍선/토스트와 중복 표시되던 것도 제거
- 알림 클릭 후 창이 복원되며 검은 화면으로 남는 버그 수정
- 고해상도(DPI) 모니터에서 아바타 이니셜이 원 밖으로 삐져나오는 문제 수정
- 대용량 파일 암·복호화에 멀티코어(프로세스 풀) 적용해 속도 개선
- Windows 시작 시 자동 실행 켜고 끄는 기능 추가
- 모든 창 항상 위에 표시 켜고 끄는 기능 추가
- 알림음 켜고 끄는 기능 추가

## v6.46 (2026-09-15)
- 설정 메뉴 렌더링 지연 개선(이모지 제거)

## v6.45 (2026-09-15)
- 포트 번호 변경 기능 추가(앱 안에서 직접 설정, 재시작 후 적용)

## v6.44 (2026-09-15)
- 파일 다이얼로그를 제외한 모든 팝업을 앱 내장 패널로 통일
- 스크롤바 스타일 통일, 아바타 원형 표시·다운로드 크기 설정 라벨 버그 수정

## v6.43 (2026-09-15)
- 독립 실행파일(exe) 빌드 지원(PyInstaller onefile)

## v6.42 (2026-09-14)
- 앱 아이콘 교체

## v6.38 ~ v6.41 (2026-09-14)
- 주요 다이얼로그를 앱 내장 패널로 전환
- 작업표시줄 알림 깜빡임 개선
- 파일 전송·다운로드 최대 크기 직접 설정 가능
- 이름 저장/스크롤·답장 기능·아바타 캐시 등 버그 다수 수정

## v6.36 ~ v6.37 (2026-09-14)
- 외부 코드 리뷰 검증에 따른 버그 약 20건 수정(포트 변경 시 자동 발견 실패, 그룹 파일 전송 오판, 실행 파일 경고 등)

## v6.33 ~ v6.34 (2026-09-14)
- 트레이 알림·답장 기능 버그 수정, 알림음 변경

## v6.20 ~ v6.28 (2026-09-13)
- 이모지/스티커 패널 UI 개선(내장화, 반응형 그리드, 로딩 속도)
- 이모지를 오리지널 스티커로 통합, 초기 기동 속도 개선
- 사이드바 갱신 매끄럽게 개선, 고해상도 작업표시줄 아이콘

## v6.15 ~ v6.19 (2026-09-13)
- 공지사항 고정, 오리지널 스티커 28종, 이모지 피커 추가
- 이모지/스티커 패널을 채팅창에 내장

## v6.9 ~ v6.14 (2026-09-13)
- 대화 로그 파일 자체 암호화
- 자동 폭파(자동 삭제) 메시지 추가(1:1 → 그룹으로 확장)
- app.py 모듈 분리, 결함 다수 수정

## v6.5 ~ v6.8.2 (2026-09-13)
- 앱 명칭 변경(LAN Talk)
- 포터블 실행 지원(저장 위치를 프로그램 폴더 기준으로 변경)
- 친구 카테고리, 여러 명에게 한 번에 보내기, 파일 이어받기(resume) 추가

## v6.0 ~ v6.4 (2026-09-12)
- 통신 암호화(HMAC) 도입
- 프로필 사진 등록/전파, 멘션(@), 전체 대화 검색, 재전송 공격 방어

## v4.0 ~ v6.1 (2026-09-11 ~ 12)
- 드래그 앤 드롭 파일 전송, 다크 테마, 클립보드 붙여넣기, 자리비움 표시 등 데스크톱 메신저 기본기 구축
